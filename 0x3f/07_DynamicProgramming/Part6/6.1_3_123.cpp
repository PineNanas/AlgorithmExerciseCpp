#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int maxProfit(vector<int>& prices) {
        

        auto dfs = [&] (this auto&& dfs, int x, int remain) -> int
        {



            int maxprofit;
            if (remain > 0)
            {
                
            }
            

            
        };
    }
};