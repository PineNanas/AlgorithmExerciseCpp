#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    vector<vector<int>> intervalIntersection(vector<vector<int>>& firstList, vector<vector<int>>& secondList) {
        int i = 0, j = 0, k = 0;
        int x = 0, y = 0, z = 0;
        int firstlen = firstList.size();
        int seclen = secondList.size();
        vector<vector<int>> ans;
        while (i < firstlen && j < seclen)
        {
            
        }
        
    }
};